"""
Problem 02: Generating Prime Sequences

Implement a function that generates the first N prime numbers.
"""


def generate_n_primes(n: int) -> list[int]:
    """
    Generates the first N prime numbers.

    Args:
        n: The number of primes to generate

    Returns:
        A list containing the first N prime numbers in ascending order
    """
    return []
